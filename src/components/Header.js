import React from 'react';
import './Header.css'; // Adicione um arquivo CSS para o estilo do header

const Header = () => {
  return (
    <header className="header">
      <h1>Plataforma de Estudos</h1>
    </header>
  );
};

export default Header;